var monster = {
    id: 1,
    name: "Bulbasaur",
    types: ["poison", "grass"]
};

// Using the monster variable above, how do we access the name?
console.log(monster.name);

// Using the monster variable above, how do we access the 2nd type?
console.log(monster.types[1]);